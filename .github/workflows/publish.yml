name: Publish

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: yarn
          registry-url: https://registry.npmjs.org
      # yarn 1 does not expand env vars in .npmrc, so write the token at runtime.
      # NPM_TOKEN reads @goodeggs packages from npm.goodeggs.com during install;
      # NPMJS_PUBLISH_TOKEN publishes this package to the public npmjs registry.
      - run: echo "//npm.goodeggs.com/:_authToken=${NPM_TOKEN}" >> .npmrc
      - run: yarn install --frozen-lockfile
      - run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPMJS_PUBLISH_TOKEN }}
